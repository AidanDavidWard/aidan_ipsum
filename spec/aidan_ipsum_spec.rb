require 'spec_helper'

describe AidanIpsum do
  it 'has a version number' do
    expect(AidanIpsum::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
